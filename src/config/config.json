{
  "development": {
    "username": "${MYSQLUSER}",
    "password": "${MYSQLPASSWORD}",
    "database": "${MYSQLDATABASE}",
    "host": "${MYSQLHOST}",
    "port": "${MYSQLPORT}",
    "dialect": "mysql"
  },
  "test": {
    "username": "${MYSQLUSER}",
    "password": "${MYSQLPASSWORD}",
    "database": "${MYSQLDATABASE}",
    "host": "${MYSQLHOST}",
    "port": "${MYSQLPORT}",
    "dialect": "mysql"
  },
  "production": {
    "username": "${MYSQLUSER}",
    "password": "${MYSQLPASSWORD}",
    "database": "${MYSQLDATABASE}",
    "host": "${MYSQLHOST}",
    "port": "${MYSQLPORT}",
    "dialect": "mysql"
  }
}